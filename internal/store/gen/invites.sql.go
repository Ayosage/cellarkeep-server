// Code generated by sqlc. DO NOT EDIT.
// versions:
//   sqlc v1.30.0
// source: invites.sql

package gen

import (
	"context"
	"time"

	"github.com/google/uuid"
)

const claimInvite = `-- name: ClaimInvite :one
update invites set used_at = $1, used_by = $2
where token = $3 and used_at is null and revoked_at is null
  and expires_at > $1
returning id, token, email, created_by, created_at, expires_at, used_at, used_by, revoked_at
`

type ClaimInviteParams struct {
	Now    *time.Time `json:"now"`
	UsedBy *uuid.UUID `json:"used_by"`
	Token  string     `json:"token"`
}

func (q *Queries) ClaimInvite(ctx context.Context, arg ClaimInviteParams) (Invite, error) {
	row := q.db.QueryRow(ctx, claimInvite, arg.Now, arg.UsedBy, arg.Token)
	var i Invite
	err := row.Scan(
		&i.ID,
		&i.Token,
		&i.Email,
		&i.CreatedBy,
		&i.CreatedAt,
		&i.ExpiresAt,
		&i.UsedAt,
		&i.UsedBy,
		&i.RevokedAt,
	)
	return i, err
}

const createInvite = `-- name: CreateInvite :one
insert into invites (token, email, created_by, created_at, expires_at)
values ($1, $2, $3, $4, $5) returning id, token, email, created_by, created_at, expires_at, used_at, used_by, revoked_at
`

type CreateInviteParams struct {
	Token     string    `json:"token"`
	Email     *string   `json:"email"`
	CreatedBy uuid.UUID `json:"created_by"`
	CreatedAt time.Time `json:"created_at"`
	ExpiresAt time.Time `json:"expires_at"`
}

func (q *Queries) CreateInvite(ctx context.Context, arg CreateInviteParams) (Invite, error) {
	row := q.db.QueryRow(ctx, createInvite,
		arg.Token,
		arg.Email,
		arg.CreatedBy,
		arg.CreatedAt,
		arg.ExpiresAt,
	)
	var i Invite
	err := row.Scan(
		&i.ID,
		&i.Token,
		&i.Email,
		&i.CreatedBy,
		&i.CreatedAt,
		&i.ExpiresAt,
		&i.UsedAt,
		&i.UsedBy,
		&i.RevokedAt,
	)
	return i, err
}

const findValidInvite = `-- name: FindValidInvite :one
select id, token, email, created_by, created_at, expires_at, used_at, used_by, revoked_at from invites
where token = $1 and used_at is null and revoked_at is null
  and expires_at > $2
`

type FindValidInviteParams struct {
	Token string    `json:"token"`
	Now   time.Time `json:"now"`
}

func (q *Queries) FindValidInvite(ctx context.Context, arg FindValidInviteParams) (Invite, error) {
	row := q.db.QueryRow(ctx, findValidInvite, arg.Token, arg.Now)
	var i Invite
	err := row.Scan(
		&i.ID,
		&i.Token,
		&i.Email,
		&i.CreatedBy,
		&i.CreatedAt,
		&i.ExpiresAt,
		&i.UsedAt,
		&i.UsedBy,
		&i.RevokedAt,
	)
	return i, err
}

const listInvites = `-- name: ListInvites :many
select i.id, i.token, i.email, i.created_by, i.created_at, i.expires_at, i.used_at, i.used_by, i.revoked_at, c.name as created_by_name, u.name as used_by_name
from invites i
join users c on c.id = i.created_by
left join users u on u.id = i.used_by
order by i.created_at desc
`

type ListInvitesRow struct {
	ID            uuid.UUID  `json:"id"`
	Token         string     `json:"token"`
	Email         *string    `json:"email"`
	CreatedBy     uuid.UUID  `json:"created_by"`
	CreatedAt     time.Time  `json:"created_at"`
	ExpiresAt     time.Time  `json:"expires_at"`
	UsedAt        *time.Time `json:"used_at"`
	UsedBy        *uuid.UUID `json:"used_by"`
	RevokedAt     *time.Time `json:"revoked_at"`
	CreatedByName string     `json:"created_by_name"`
	UsedByName    *string    `json:"used_by_name"`
}

func (q *Queries) ListInvites(ctx context.Context) ([]ListInvitesRow, error) {
	rows, err := q.db.Query(ctx, listInvites)
	if err != nil {
		return nil, err
	}
	defer rows.Close()
	var items []ListInvitesRow
	for rows.Next() {
		var i ListInvitesRow
		if err := rows.Scan(
			&i.ID,
			&i.Token,
			&i.Email,
			&i.CreatedBy,
			&i.CreatedAt,
			&i.ExpiresAt,
			&i.UsedAt,
			&i.UsedBy,
			&i.RevokedAt,
			&i.CreatedByName,
			&i.UsedByName,
		); err != nil {
			return nil, err
		}
		items = append(items, i)
	}
	if err := rows.Err(); err != nil {
		return nil, err
	}
	return items, nil
}

const revokeInvite = `-- name: RevokeInvite :one
update invites set revoked_at = $1
where id = $2 and used_at is null and revoked_at is null
returning id, token, email, created_by, created_at, expires_at, used_at, used_by, revoked_at
`

type RevokeInviteParams struct {
	Now *time.Time `json:"now"`
	ID  uuid.UUID  `json:"id"`
}

func (q *Queries) RevokeInvite(ctx context.Context, arg RevokeInviteParams) (Invite, error) {
	row := q.db.QueryRow(ctx, revokeInvite, arg.Now, arg.ID)
	var i Invite
	err := row.Scan(
		&i.ID,
		&i.Token,
		&i.Email,
		&i.CreatedBy,
		&i.CreatedAt,
		&i.ExpiresAt,
		&i.UsedAt,
		&i.UsedBy,
		&i.RevokedAt,
	)
	return i, err
}
